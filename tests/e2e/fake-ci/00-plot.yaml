---
apiVersion: director.kuberik.io/v1
kind: Plot
metadata:
  name: fake-ci
spec:
  frames:
    - name: clone
      action:
        template:
          spec:
            containers:
              - name: clone
                image: alpine:latest
                command: [sh, -c]
                args: ["echo cloning... && sleep 5"]
    - name: build
      causedBy:
        - clone
      action:
        template:
          spec:
            containers:
              - name: build
                image: alpine:latest
                command: [sh, -c]
                args: ["echo building... && sleep 5"]
